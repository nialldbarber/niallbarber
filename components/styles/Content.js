import styled from 'styled-components';

const ContentInfo = styled.div`
	position: relative;
	max-width: 540px;
	margin-left: auto;
	p {
		font-size: 2rem;
		&:first-child {
			margin-top: 0;
		}
	}
`;

export default ContentInfo;
